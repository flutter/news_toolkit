name: ads_consent_client

concurrency:
  group: ${{#mustacheCase}}github.workflow{{/mustacheCase}}-${{#mustacheCase}}github.ref{{/mustacheCase}}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "packages/ads_consent_client/**"
      - ".github/workflows/ads_consent_client.yaml"
    branches:
      - main

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      flutter_version: {{flutter_version}}
      working_directory: packages/ads_consent_client
      coverage_excludes: "lib/src/generated/*.dart"
