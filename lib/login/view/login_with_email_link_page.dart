import 'package:flutter/material.dart';

class LoginWithEmailLinkPage extends StatelessWidget {
  const LoginWithEmailLinkPage({Key? key}) : super(key: key);

  static Route route() =>
      MaterialPageRoute<void>(builder: (_) => const LoginWithEmailLinkPage());

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
