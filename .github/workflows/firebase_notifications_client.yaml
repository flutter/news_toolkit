name: firebase_notifications_client

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "packages/firebase_notifications_client/firebase_notifications_client/**"
      - ".github/workflows/firebase_notifications_client.yaml"
    branches:
      - main

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/dart_package.yml@v1
    with:
      working_directory: packages/firebase_notifications_client/firebase_notifications_client
