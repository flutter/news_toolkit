name: notification_preferences_repository

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "api/packages/notification_preferences_repository/**"
      - ".github/workflows/notification_preferences_repository.yaml"
    branches:
      - main

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/dart_package.yml@v1
    with:
      coverage_excludes: "**/*.g.dart"
      working_directory: api/packages/notification_preferences_repository
